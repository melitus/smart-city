export enum TopicNames {
  BUS_LOCATION_DATA = 'bus-location-data',
  VAN_LOCATION_DATA = "van-location-data",
  WEATHER_UPDATES = 'weather-updates',
  PASSENGER_WAITING_DATA = 'passenger-waiting-data',
}

export enum EventType {
  PRODUCER = 'producer',
  CONSUMER = 'consumer',
}

